[project]
name = "intens"
version = "0.1.0"
description = "REST and ZMQ utilities for intens"
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = [
    "pyzmq>=27.1.0",
    "requests>=2.32.5",
]
