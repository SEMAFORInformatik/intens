// **********************************************************************
// LogBook
// **********************************************************************

logbook_in{JSON} (
  logbook
);

logbook_text_out{JSON} (
  logbookInfo.text
);

logbook_pos_out{JSON} (
  logbookInfo.position
);
