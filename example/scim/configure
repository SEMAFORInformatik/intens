#!/bin/sh
#
# call this script from a build directory

BASEDIR=$(dirname "$0")

if [ "${OSTYPE}" = "msys" ]; then
    cmake -G "MSYS Makefiles" -DCMAKE_C_COMPILER=gcc -S ${BASEDIR}
else
    cmake -S ${BASEDIR}
fi
